version: "3.1"
services:
  rust-dev:
    container_name: control-sys-rs
    build:
      context: .
    restart: "unless-stopped"
    network_mode: "host"
    privileged: true
    volumes:
      - ~/.ssh:/root/.ssh
    environment:
      - DISPLAY=${DISPLAY}
    tty: true
